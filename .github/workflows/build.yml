name: Build

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable
      - name: Show Xcode version
        run: xcodebuild -version
      - name: Build TinyCSV
        run: xcodebuild clean build -project TinyCSV.xcodeproj -scheme TinyCSV -configuration Release -derivedDataPath /tmp/tinybuild/tinycsv CODE_SIGN_IDENTITY="-"
